import Card from '@/app/componans/Card';
import React from 'react'

export default async function Search({params}) {
  
    const search = params.searchId;
    console.log(search);

    const res = fetch(``)
    return (
    <div>
        <h1></h1>
    </div>
  )
}
